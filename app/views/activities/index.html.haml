- title_helper "Listing activities"


- @activities.each do |group_name, activities|
  %h3
    = group_name
    = link_to("+", new_activity_path(:activity_type => group_name.downcase.pluralize))
  %ul
    - for activity in activities
      %li= link_to activity.title, edit_activity_path(activity.class.name.downcase.pluralize, activity.identifier)

%ul
  %li= link_to 'New grant', new_activity_path(:activity_type => "grants")
  %li= link_to 'New publication', new_activity_path(:activity_type => "publications")
  %li= link_to 'New presentation', new_activity_path(:activity_type => "presentations")
  %li= link_to 'New service', new_activity_path(:activity_type => "services")
